import UnitDetails from "@/components/pages/unit/UnitDetails";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "จัดการหน่วย | Chiang Mai University",
};

export default function Page() {
  return <UnitDetails />;
}
